# Alembic migrations

Placeholder for production PostgreSQL migrations.

The current MVP backend persists data in local JSON storage so the frontend can integrate against stable API contracts before the DB pass.
